# ChatGPT review request: PR #6

Please perform a read-only code review of:

<https://github.com/amitkarpe/nextflow-offline/pull/6>

Review the latest PR state and commit `75de8b8` against `main`.

Context: this is a focused Phase 1 offline Nextflow bundle path for
`nf-core/demo` 1.0.2. The normal fast path uses the existing private S3 cache,
the `rnaseq-tiny-20260624` local fixture, Podman, and the repo-owned
`podman,offline_smoke` profile.

Review in this order:

1. Blocking/high-severity correctness or safety findings.
2. Whether the custom profile and `-offline` runtime avoid public dependencies.
3. Shell robustness, image archive matching/loading, plugin and Nextflow
   version pinning, and S3 source/publication boundaries.
4. Reproducibility and whether the cache-first path is materially faster.
5. Documentation clarity and scope creep.

Important scope decisions:

- Podman is the only smoke-test engine for this fast path.
- Docker smoke testing is intentionally excluded.
- Bundle checksum verification is intentionally excluded from the smoke gate.
- S3 publication is disabled by default and was not performed for this proof.
- Sarek and offline-server/EC2 execution are deferred.

Please return concise findings with severity, exact file/line references, and
the smallest recommended fixes. State explicitly whether any blocking finding
exists. Do not modify the repository or broaden the design.
